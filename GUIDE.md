# 장인 사령부 — 아이패드 PWA 테스트 가이드

Mac 없이 윈도우 PC + 아이패드만으로 **알림까지 포함**해 테스트합니다.

## 준비물
- 아이패드 (iPadOS 16.4 이상 ✓)
- 윈도우 PC에 **Node.js** 설치 (이미 있음)
- 아이패드와 PC를 **같은 네트워크**로 연결
  - 방법 A: 둘 다 같은 와이파이 (가장 쉬움)
  - 방법 B: 아이패드를 USB로 PC에 꽂고 **설정 → USB 테더링/개인용 핫스팟** 켜기

## 실행 (3단계)

### 1. PC에서 서버 켜기
이 폴더에서 터미널(PowerShell 또는 CMD) 열고:
```
node serve.js
```
→ 화면에 `https://192.168.x.x:8443` 같은 주소가 뜹니다. 그 주소를 적어두세요.

> openssl 경고가 뜨면: Git이 깔려 있으면 "Git Bash"에서 `node serve.js` 실행하세요.
> (Git Bash에는 openssl이 포함돼 있어 인증서가 자동 생성됩니다.)

### 2. 아이패드에서 접속
1. 아이패드 **사파리**(꼭 사파리)에서 위 주소 입력
2. "안전하지 않은 연결" 경고 → **[고급] → [웹사이트 방문]** (자체 서명 인증서라 정상)
3. 앱 화면이 뜨면 성공

### 3. 홈 화면에 추가 → 알림 켜기 (★ 알림의 핵심)
1. 사파리 하단 **공유 버튼**(□↑) 탭
2. **[홈 화면에 추가]** 선택 → 추가
3. **사파리를 닫고**, 홈 화면의 "장인 사령부" 아이콘으로 다시 열기
   (← 이 단계가 필수. 사파리 탭에서는 iOS가 알림을 막습니다)
4. 앱에서 계정을 가져오면 알림 권한 요청이 뜸 → **허용**

## 알림 테스트 빠르게 해보기
실제 8초짜리 업그레이드를 기다리긴 어려우니, 테스트용으로:
- 게임 JSON을 가져온 뒤, 가장 짧은 타이머가 끝날 때 알림이 오는지 확인
- 또는 짧은 더미 데이터로 확인하고 싶으면 Claude에게 "1분 뒤 울리는 테스트 알림 버튼 넣어줘"라고 요청

## 알아둘 점 (iOS PWA 알림의 한계)
- 아이패드가 깊은 절전이거나 완전히 꺼져 있으면 정확한 초에 안 올 수 있어요. 앱을 다시 열면 "완료됐어야 할" 알림이 그때 뜹니다.
- 정확한 백그라운드 알림은 결국 네이티브 앱(App Store)에서만 100% 보장됩니다. 이건 화면·사용감 + 알림 동작 원리 확인용입니다.

## 안 될 때
- 주소가 안 열림 → PC 방화벽이 8443 포트를 막았을 수 있음. 방화벽에서 Node.js 허용.
- 같은 와이파이인데 안 됨 → 공용 와이파이는 기기 간 통신을 차단하기도 함. USB 테더링(방법 B)으로 시도.
- 알림 권한 요청이 안 뜸 → 반드시 "홈 화면 아이콘"으로 연 상태인지 확인.
